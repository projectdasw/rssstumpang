name: Deploy RSS Stumpang

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy Website
        run: |
          cd /var/www/html/rssstumpang

        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2222
          git reset --hard
          git clean -fd
          git pull origin main
          sudo systemctl restart apache2
